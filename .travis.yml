language: csharp
mono: none
dotnet: 3.1.401

script:
  - dotnet restore
  - dotnet build
  - dotnet test
  - dotnet pack -c Release -o packages

deploy:
  skip_cleanup: true
  provider: script
  script: ./deploy.sh $NUGET_KEY
  on:
    branch: master
    tags: true
