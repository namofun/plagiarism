<Project>

  <Import Project="NugetPackage.Common.props" />

  <PropertyGroup>
    <Authors>yang-er</Authors>
    <Description>UI kit for ASP.NET Core with moduling support. $(DescriptionAttach)</Description>
    <Version>1.0.0</Version>
    <PackageVersion>1.2.$(PreviewBuild)</PackageVersion>
    <AssemblyVersion>$(AspNetCoreVersion).0</AssemblyVersion>
    <FileVersion>$(AspNetCoreVersion).0</FileVersion>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
  </PropertyGroup>

</Project>
