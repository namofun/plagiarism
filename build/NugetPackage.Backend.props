<Project>

  <Import Project="NugetPackage.Common.props" />

  <PropertyGroup>
    <Authors>ychlgdr;SAILgaosai;yang-er</Authors>
    <Description>Plagiarism detection modules. $(DescriptionAttach)</Description>
    <PackageVersion>1.2.$(PreviewBuild)</PackageVersion>
    <Version>$(PackageVersion)</Version>
    <AssemblyVersion>$(PackageVersion).0</AssemblyVersion>
    <FileVersion>$(PackageVersion).0</FileVersion>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
  </PropertyGroup>

</Project>
