<Project>

  <!--
  The item is hidden but will be copied to corresponding folders.
  Wanted to keep the document XML always be present so that we can discover it for swagger-gen.
  -->
  <ItemGroup>
    <ContentWithTargetPath Include="$(MSBuildThisFileDirectory)\..\lib\netcoreapp3.1\SatelliteSite.PlagModule.xml" TargetPath="SatelliteSite.PlagModule.xml" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>

</Project>