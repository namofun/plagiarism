@using SatelliteSite.Data
@model Submission
@{
    ViewData["Title"] = $"Submission s{Model.Id}: {Model.Name}";
}

<h2 class="mt-2">Submission s@(Model.Id): <a asp-action="SourceCode" asp-route-sid="@Model.Id">@Model.Name</a></h2>
<datatable data="ViewBag.Reports" />
