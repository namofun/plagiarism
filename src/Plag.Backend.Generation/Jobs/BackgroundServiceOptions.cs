namespace Plag.Backend.Jobs
{
    public class BackgroundServiceOptions
    {
        /// <summary>
        /// Whether to use batch processing in report.
        /// </summary>
        public bool UseBatchInReport { get; set; } = true;
    }
}
